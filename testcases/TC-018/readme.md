# Testcase title: Comparing addresses

## Description

This scenario checks compares addresses and type of addresses

### Expected results

The ordering of addresses is as follows:
tz1 < tz2 < tz3 < tz4 < kt1 < sr1

Within the same type of addresses the addresses are ordered lexigraphically.
