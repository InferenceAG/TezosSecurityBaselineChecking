# Testcase title: Smart optimistic rollup - sending wrong input parameter type

## Description
To check whether a smart optimistic rollup accepts a wrong input parameter

## Background
Smart optimisitc rollups are expecting BYTES as the only input parameter. Thus, a smart optimistic rollup interface is of type "CONTRACT BYTES".
